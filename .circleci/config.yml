version: 2
jobs:
  build:
    machine: true
    working_directory: ~/repo

    environment:
      FLASK_ENV: test
      FLASK_APP: microblog.py
    steps:
      - checkout
      - run:
          name: run docker tests
          command: |
            make test